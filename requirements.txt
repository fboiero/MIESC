# MIESC Core runtime dependencies
# For optional extras and full CLI/Docker profiles, see pyproject.toml
# For pinned versions, see requirements-lock.txt

pydantic>=2.0.0
pyyaml>=6.0.0
jinja2>=3.1.0
click>=8.1.0
slither-analyzer>=0.10.0
rich>=13.0.0
