# MIESC Environment Configuration
# Copy this file to .env and customize for your setup
# See docs/INSTALLATION.md for detailed configuration guide

# =============================================================================
# CORE SETTINGS
# =============================================================================

# Debug mode (true/false) - enables verbose logging
MIESC_DEBUG=false

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
MIESC_LOG_LEVEL=INFO

# Output directory for reports and analysis results
MIESC_OUTPUT_DIR=./output

# Cache directory for tool artifacts
MIESC_CACHE_DIR=./.miesc_cache

# =============================================================================
# LLM CONFIGURATION (Local - Ollama)
# =============================================================================

# Ollama server URL (default: http://localhost:11434)
# For Docker on macOS: http://host.docker.internal:11434
# For Docker on Linux with --network host: http://localhost:11434
OLLAMA_HOST=http://localhost:11434

# Default model for analysis (must be pulled first: ollama pull <model>)
OLLAMA_MODEL=deepseek-coder:6.7b

# Alternative models (optional)
# OLLAMA_MODEL_CODELLAMA=codellama
# OLLAMA_MODEL_MISTRAL=mistral

# =============================================================================
# LLM CONFIGURATION (Cloud - Optional)
# =============================================================================

# OpenAI API key (for GPTScan, GPT-based analysis)
# Get your key at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Anthropic API key (for Claude-based analysis)
# Get your key at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-...

# =============================================================================
# EXTERNAL TOOL CONFIGURATION
# =============================================================================

# Certora Prover API key (for formal verification)
# Get your key at: https://www.certora.com/
# CERTORA_KEY=...

# Etherscan API key (for contract verification/fetching)
# Get your key at: https://etherscan.io/apis
# ETHERSCAN_API_KEY=...

# =============================================================================
# SOLIDITY COMPILER
# =============================================================================

# Solidity compiler version (managed by solc-select)
# SOLC_VERSION=0.8.20

# Path to custom solc binary (optional)
# SOLC_BINARY=/usr/local/bin/solc

# =============================================================================
# DATABASE (Optional - for persistent storage)
# =============================================================================

# Database URL (SQLite by default, PostgreSQL optional)
# DATABASE_URL=sqlite:///./miesc.db
# DATABASE_URL=postgresql://user:password@localhost:5432/miesc

# =============================================================================
# WEB SERVER (for API/Dashboard)
# =============================================================================

# API server host and port
MIESC_API_HOST=0.0.0.0
MIESC_API_PORT=8000

# Streamlit dashboard port
MIESC_DASHBOARD_PORT=8501

# =============================================================================
# DOCKER SETTINGS
# =============================================================================

# Docker platform override (for ARM/x86 compatibility)
# DOCKER_DEFAULT_PLATFORM=linux/amd64

# Full image platform (for ARM users building locally)
# MIESC_FULL_PLATFORM=linux/arm64

# =============================================================================
# SECURITY
# =============================================================================

# Disable telemetry (no data is collected by default)
MIESC_TELEMETRY_DISABLED=true

# Maximum file size for analysis (in MB)
MIESC_MAX_FILE_SIZE=10

# Analysis timeout per tool (in seconds)
MIESC_TOOL_TIMEOUT=300

# =============================================================================
# RAG CONFIGURATION (Retrieval-Augmented Generation)
# =============================================================================

# ChromaDB persistence directory
MIESC_CHROMADB_DIR=~/.miesc/chromadb

# Embedding model for RAG
MIESC_EMBEDDING_MODEL=all-MiniLM-L6-v2

# =============================================================================
# MCP SERVER (Model Context Protocol)
# =============================================================================

# MCP server mode (stdio, sse, websocket)
# MCP_SERVER_MODE=stdio

# MCP server port (for sse/websocket modes)
# MCP_SERVER_PORT=3000
